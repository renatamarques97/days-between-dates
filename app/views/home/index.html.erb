<h1>Days between Dates</h1>

<div>
  <div class="form-wrapper">
    <%= form_for :search, method: :get do |t| %>
      <%= t.date_field :start_date, id: "start_date" %>
      <%= t.date_field :end_date, id: "end_date" %>
      <%= t.submit "Submit" %>
    <% end %>
  </div>
</div>

